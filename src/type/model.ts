export type AlbumPreview = {
  id: string;
  title: string;
  artistName: string;
  coverSrc: string;
  saved: boolean;
};

export type TrackPreview = {
  id: string;
  title: string;
  artistName: string;
  audioSrc: string;
};
