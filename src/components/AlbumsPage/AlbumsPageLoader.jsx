// @flow strict

import * as React from "react";

import styles from "./AlbumsPage.module.css";

type TProps = {||};

export const AlbumsPageLoader = (props: TProps) => (
  <div className={styles.AlbumsPage__Loader} />
);
